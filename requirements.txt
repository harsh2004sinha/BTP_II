# Core scientific
numpy>=1.24.0
scipy>=1.10.0

# Solar modelling
pvlib>=0.10.0

# Optimization
pyomo>=6.5.0

# RL (optional — falls back to rule-based if not installed)
stable-baselines3>=2.0.0
gymnasium>=0.29.0

# Explainability (optional — falls back to domain rules)
shap>=0.42.0

# Data handling
pandas>=2.0.0

# PDF bill parsing (backend)
pdfplumber>=0.9.0

# API (backend)
fastapi>=0.100.0
uvicorn>=0.23.0
python-jose>=3.3.0
bcrypt>=4.0.1
python-multipart>=0.0.6
sqlalchemy>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0

# Deployment
psycopg2-binary>=2.9.9
gunicorn>=21.2.0

apscheduler
pydantic_settings
psycopg2
aiofiles
pydantic[email]
pymupdf
pypdf2